import { Meta } from "@storybook/addon-docs";
import { SearchForm } from "./PriceLookup";

<Meta title="Tools/PriceLookup" component={SearchForm} />

# Price lookup

Price lookup lets users look into active price lists.
The user can fill out from and to with optional dates. There should at least be defined an item.
